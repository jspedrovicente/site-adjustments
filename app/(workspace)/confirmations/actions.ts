"use server";
import { timingSafeEqual } from "node:crypto";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export async function finalizeDemand(formData: FormData) { const id = formData.get("id"), password = formData.get("password"), expected = process.env.COMPLETION_PASSWORD; if (typeof id !== "string" || typeof password !== "string") redirect("/confirmations?error=invalid"); if (!expected) redirect("/confirmations?error=not-configured"); const received = Buffer.from(password), configured = Buffer.from(expected); if (received.length !== configured.length || !timingSafeEqual(received, configured)) redirect("/confirmations?error=invalid-password"); const supabase = await createClient(); const { data: items, error: readError } = await supabase.from("adjustment_items").select("semantics").eq("demand_id", id); if (readError || !items?.length) redirect("/confirmations?error=update-failed"); const allDone = items.every((item) => Array.isArray(item.semantics) && item.semantics.some((value) => value === "done" || (typeof value === "object" && value !== null && !Array.isArray(value) && (value.semantic === "done" || value.semantic === "completion")))); if (!allDone) redirect("/confirmations?error=items-pending"); const { data, error } = await supabase.from("adjustment_demands").update({ status: "Finalizada", updated_at: new Date().toISOString() }).eq("id", id).select("id").maybeSingle(); if (error || !data) redirect("/confirmations?error=update-failed"); revalidatePath("/confirmations"); revalidatePath("/demands"); revalidatePath("/dashboard"); redirect("/confirmations?finalized=true"); }
