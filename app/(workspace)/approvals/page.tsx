import Link from "next/link";
import { ShieldCheck } from "lucide-react";
import { getDemands } from "@/lib/data/demands";
import { PageHeader } from "@/components/page-header";
import { approveDemand, rejectDemand } from "./actions";
import { isDemandPendingAnalysis } from "@/lib/data/model";
type Params = Promise<Record<string,string|string[]|undefined>>;
export default async function ApprovalsPage({ searchParams }: { searchParams: Params }) { const params = await searchParams; const demands = (await getDemands()).filter(isDemandPendingAnalysis); const error = Array.isArray(params.error) ? params.error[0] : params.error; const message = error === "not-configured" ? "Configure APPROVAL_PASSWORD no .env.local." : error === "invalid-password" ? "Senha de análise incorreta." : error === "reason-required" ? "Informe uma justificativa para reprovar a demanda." : error ? "Não foi possível registrar a análise. Verifique as permissões RLS." : null; return <><PageHeader eyebrow="Revisão" title="Demandas pendentes de análise" description={`${demands.length} aguardando sua análise`}/><main className="space-y-4 p-5 sm:p-8 lg:p-10">{message && <div className="rounded-md border border-red-200 bg-red-50 p-4 text-sm font-medium text-red-700">{message}</div>}{params.created && <div className="rounded-md border border-emerald-200 bg-emerald-50 p-4 text-sm font-medium text-emerald-700">Demanda criada e enviada para análise.</div>}{params.rejected && <div className="rounded-md border border-amber-200 bg-amber-50 p-4 text-sm font-medium text-amber-800">Demanda reprovada e movida para Demandas reprovadas.</div>}{demands.map((demand) => <article key={demand.id} className="panel rounded-lg p-5"><div><p className="text-xs font-semibold uppercase tracking-wide text-amber-700">{demand.category?.name ?? "Sem categoria"}</p><Link href={`/demands/${demand.id}`} className="mt-1 block text-lg font-semibold text-slate-950 hover:text-blue-700">{demand.title}</Link><p className="mt-2 text-sm text-slate-500">{demand.items.length} itens · Responsável: {demand.developer ?? "Não atribuído"}</p></div><div className="mt-5 grid gap-3 border-t pt-5 lg:grid-cols-2"><form action={approveDemand} className="flex flex-col gap-2 sm:flex-row"><input type="hidden" name="id" value={demand.id}/><input type="password" name="password" required placeholder="Senha de análise" className="focus-ring min-w-0 flex-1 rounded-md border px-3 py-2.5 text-sm"/><button className="focus-ring flex items-center justify-center gap-2 rounded-md bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white"><ShieldCheck className="size-4"/>Aprovar demanda</button></form><details className="rounded-md border border-red-200 bg-red-50"><summary className="cursor-pointer px-4 py-2.5 text-sm font-semibold text-red-700">Reprovar demanda</summary><form action={rejectDemand} className="space-y-2 border-t border-red-200 p-3"><input type="hidden" name="id" value={demand.id}/><textarea name="reason" required placeholder="Justificativa da reprovação" className="focus-ring min-h-20 w-full rounded-md border bg-white px-3 py-2 text-sm"/><div className="flex gap-2"><input type="password" name="password" required placeholder="Senha de análise" className="focus-ring min-w-0 flex-1 rounded-md border bg-white px-3 py-2 text-sm"/><button className="rounded-md bg-red-700 px-4 py-2 text-sm font-semibold text-white">Confirmar reprovação</button></div></form></details></div></article>)}{!demands.length && <div className="panel rounded-lg p-10 text-center"><ShieldCheck className="mx-auto size-8 text-emerald-600"/><p className="mt-3 font-semibold">Nenhuma demanda aguardando análise</p></div>}</main></>; }
