import Link from "next/link";
import { Ban } from "lucide-react";
import { getDemands } from "@/lib/data/demands";
import { isDemandRejected } from "@/lib/data/model";
import { PageHeader } from "@/components/page-header";
export default async function RejectedPage() { const demands = (await getDemands()).filter(isDemandRejected); return <><PageHeader eyebrow="Histórico de análise" title="Demandas reprovadas" description={`${demands.length} demandas reprovadas`}/><main className="space-y-4 p-5 sm:p-8 lg:p-10">{demands.map((demand) => <article key={demand.id} className="panel rounded-lg p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-red-700">{demand.category?.name ?? "Sem categoria"}</p><Link href={`/demands/${demand.id}`} className="mt-1 block text-lg font-semibold text-slate-950 hover:text-blue-700">{demand.title}</Link><p className="mt-2 text-sm text-slate-500">{demand.items.length} itens · Responsável: {demand.developer ?? "Não atribuído"}</p></div><span className="inline-flex w-fit items-center gap-2 rounded-full bg-red-100 px-3 py-1.5 text-xs font-semibold text-red-700"><Ban className="size-4"/>Reprovada</span></div><div className="mt-5 rounded-md border border-red-200 bg-red-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-red-700">Justificativa</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-red-950">{demand.validationNotes ?? "Justificativa não informada"}</p></div></article>)}{!demands.length && <div className="panel rounded-lg p-10 text-center"><Ban className="mx-auto size-8 text-slate-400"/><p className="mt-3 font-semibold">Nenhuma demanda reprovada</p></div>}</main></>; }
