import Link from "next/link";
import { CalendarDays, CircleHelp, ImageIcon, UserRound } from "lucide-react";
import type { Demand } from "@/lib/data/model";
import { PriorityBadge, StatusBadge } from "./badges";
const date = (value?: string) => value ? new Intl.DateTimeFormat("pt-BR", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(value)) : "—";
export function DemandCard({ demand }: { demand: Demand }) { const questions = demand.items.filter((item) => item.type === "question").length; const images = demand.items.reduce((sum, item) => sum + item.attachments.length, 0); return <Link href={`/demands/${demand.id}`} className="panel focus-ring block rounded-lg p-5 transition hover:border-slate-300 hover:shadow-md"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="mb-2 flex flex-wrap items-center gap-2"><span className="text-xs font-medium text-blue-700">{demand.category?.name ?? "Sem categoria"}</span>{demand.sourceId && <span className="text-xs text-slate-400">#{demand.sourceId}</span>}</div><h2 className="font-semibold text-slate-950">{demand.title}</h2></div><div className="flex shrink-0 gap-2"><PriorityBadge value={demand.priority} /><StatusBadge value={demand.status} /></div></div><div className="mt-5 grid gap-3 border-t pt-4 text-xs text-slate-500 sm:grid-cols-4"><span className="flex items-center gap-2"><UserRound className="size-4" />{demand.developer ?? "Não atribuído"}</span><span className="flex items-center gap-2"><CircleHelp className="size-4" />{demand.items.length} itens · {questions} dúvidas</span><span className="flex items-center gap-2"><ImageIcon className="size-4" />{images} imagens</span><span className="flex items-center gap-2"><CalendarDays className="size-4" />{date(demand.updatedAt)}</span></div></Link>; }
