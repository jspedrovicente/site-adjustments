import { cn } from "@/lib/utils";
export function PriorityBadge({ value }: { value: string }) { const key = value.toLowerCase(); return <span className={cn("inline-flex rounded-full border px-2.5 py-1 text-xs font-semibold", key.includes("alta") ? "border-red-200 bg-red-50 text-red-700" : key.includes("média") || key.includes("media") ? "border-amber-200 bg-amber-50 text-amber-800" : "border-slate-200 bg-slate-50 text-slate-600")}>{value}</span>; }
export function StatusBadge({ value }: { value: string }) { const key = value.toLowerCase(); const cls = key.includes("aprov") || key.includes("concl") ? "border-emerald-200 bg-emerald-50 text-emerald-700" : key.includes("andamento") || key.includes("validar") ? "border-blue-200 bg-blue-50 text-blue-700" : key.includes("corrigir") ? "border-red-200 bg-red-50 text-red-700" : "border-slate-200 bg-slate-50 text-slate-600"; return <span className={cn("inline-flex rounded-full border px-2.5 py-1 text-xs font-medium", cls)}>{value}</span>; }
