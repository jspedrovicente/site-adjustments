"use client";
/* eslint-disable @next/next/no-img-element -- signed Storage URLs have runtime hosts and screenshot dimensions */
import { useEffect, useState } from "react";
import { X, ZoomIn } from "lucide-react";
export function PreviewImage({ src, alt }: { src: string; alt: string }) { const [open,setOpen] = useState(false); useEffect(() => { const close = (event: KeyboardEvent) => event.key === "Escape" && setOpen(false); window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close); }, []); return <><button type="button" onClick={() => setOpen(true)} className="group relative block w-full overflow-hidden rounded-md border bg-slate-50"><img src={src} alt={alt} className="max-h-[520px] w-full object-contain"/><span className="absolute bottom-2 right-2 rounded bg-slate-950/75 p-2 text-white opacity-0 transition group-hover:opacity-100"><ZoomIn className="size-4"/></span></button>{open && <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-slate-950/90 p-4" onClick={() => setOpen(false)}><button className="absolute right-5 top-5 rounded-full bg-white p-2 text-slate-950" aria-label="Fechar"><X className="size-5"/></button><img src={src} alt={alt} className="max-h-[92vh] max-w-[96vw] object-contain" onClick={(e) => e.stopPropagation()}/></div>}</>; }
